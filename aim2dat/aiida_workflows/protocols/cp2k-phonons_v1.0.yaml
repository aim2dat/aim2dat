%YAML 1.2
---
# Post-optimization phonon pipeline (crystals + MOFs). Merged 2026-07-15 from
# cp2k-crystal-phonons-only_v1.0 (Si/NaCl-validated) and cp2k-phonon-mofs_v1.0
# (UiO-66-validated) -- both are now user_input (general_input is NOT
# overridable via set_user_input -- only user_input ports are,
# workflow_builder.py:set_user_input). numerical_p.kpoints_ref_dist keeps a
# 0.15 default (both source files always baked *some* value there); its
# force.* counterpart deliberately has NO default -- a baked default there
# gets applied even when never explicitly set (confirmed: WorkflowBuilder
# materializes it at protocol-load time, `optional: true` doesn't stop
# that), which would silently turn on a k-point mesh for the Si/NaCl force
# evals that were validated Gamma-only. See the gotcha below.
title: cp2k-phonons
version: '1.0'
description: >
  Finite-displacement phonon band structure + DOS on an already-optimized
  StructureData parent_node: (1) seekpath_analysis -- primitive cell +
  q-point band path, once; (2) find_scf_parameters -- fresh SCF search on
  the primitive cell; (3) phonons -- PhononWorkChain fan-out.
dependencies:
- [aiida-core, '1.6', Null]
- [seekpath, '2.0', Null]
parent_node_type: structure
parent_node_input:
  seekpath_analysis: structure
tasks:
  seekpath_analysis:
    process: aim2dat.seekpath.structure_analysis
  find_scf_parameters:
    process: aim2dat.cp2k.find_scf_p
    blacklist_inputs:
    - custom_scf_method
    dependencies:
      seekpath_analysis:
      - [primitive_structure, structural_p.structure]
  phonons:
    process: aim2dat.cp2k.combined.phonons
    dependencies:
      seekpath_analysis:
      - [primitive_structure, phonopy_p.structure]
      - [parameters, phonopy_p.path_parameters]
      find_scf_parameters:
      - [scf_parameters, force.structural_p.scf_parameters]

general_input:
  cp2k.parameters:
    value:
      GLOBAL:
        PRINT_LEVEL: MEDIUM
        PREFERRED_DIAG_LIBRARY: SCALAPACK
        EXTENDED_FFT_LENGTHS: true
      FORCE_EVAL:
        METHOD: Quickstep
        STRESS_TENSOR: ANALYTICAL
        DFT:
          MGRID: {}
          POISSON: {PERIODIC: XYZ, POISSON_SOLVER: PERIODIC}
          BASIS_SET_FILE_NAME: [BASIS_MOLOPT_UZH, BASIS_MOLOPT, BASIS_MOLOPT_UCL]
          POTENTIAL_FILE_NAME: GTH_POTENTIALS
          QS: {EXTRAPOLATION: USE_GUESS, EPS_DEFAULT: 1.0e-14}
          SCF: {EPS_SCF: 5.0e-7}
          KPOINTS:
            EPS_GEO: 1.0e-8
            FULL_GRID: false
            SYMMETRY: false
          XC: {}
        SUBSYS:
          CELL: {PERIODIC: XYZ}
    aiida_node: true
    tasks: [find_scf_parameters]
  scf_method:
    value: density_mixing
    aiida_node: true
    tasks: [find_scf_parameters]
  force.scf_method:
    value: density_mixing
    aiida_node: true
    tasks: [phonons]
  factor_unocc_states:
    value: 0.75
    aiida_node: true
    tasks: [find_scf_parameters]
  force.factor_unocc_states:
    value: 0.75
    aiida_node: true
    tasks: [phonons]
  enable_roks:
    value: false
    aiida_node: true
    tasks: [find_scf_parameters]
  force.enable_roks:
    value: false
    aiida_node: true
    tasks: [phonons]
  always_add_unocc_states:
    value: true
    aiida_node: true
    tasks: [find_scf_parameters]
  max_iterations:
    value: 100
    aiida_node: true
    compare: false
    tasks: [find_scf_parameters]
  force.max_iterations:
    value: 100
    aiida_node: true
    compare: false
    tasks: [phonons]
  phonopy_p.symprec:
    value: 0.005
    aiida_node: true
    tasks: [phonons]
  phonopy_p.dos_mesh:
    value: [20, 20, 20]
    aiida_node: true
    tasks: [phonons]

user_input:
  # k-point density (1/A) for find_scf_parameters. Semiconductor/insulator
  # default; MOFs' larger cells typically want ~0.20 -- override via
  # set_user_input("numerical_p.kpoints_ref_dist", 0.20).
  numerical_p.kpoints_ref_dist:
    validation: aim2dat.cp2k.find_scf_p
    aiida_node: true
    optional: true
    compare: false
    tasks: [find_scf_parameters]
    value: 0.15
  # Left unset (no default here, deliberately): Gamma-only force evals, only
  # safe when the supercell folds the BZ on its own (Si/NaCl 3x3x3 -- the
  # validated recipe this port must not silently change for). Whenever the
  # supercell does NOT fold the BZ, this MUST be set to match
  # numerical_p.kpoints_ref_dist (the k-sampling the parent_node structure
  # was relaxed with) -- e.g. MOFs: set_user_input(
  # "force.numerical_p.kpoints_ref_dist", 0.20). Left mismatched, the
  # reference/displacement force evals run on a different PES than the
  # cell-opt and the reference-force gate (PhononWorkChain exit 402) fails
  # on any structure with free Wyckoff parameters (UiO-66 2026-07-07:
  # max |F| 5.8e-4 vs < 2e-5 Ha/Bohr).
  force.numerical_p.kpoints_ref_dist:
    validation: aim2dat.cp2k.combined.phonons
    aiida_node: true
    optional: true
    compare: false
    tasks: [phonons]
  # Overrides phonopy_p.structure for the phonons task only (arrow syntax
  # beats the seekpath_analysis dependency wire). Needed when the seekpath
  # structure must be re-relaxed on the force-eval PES first -- a structure
  # relaxed in one cell setting is not force-free when re-expressed in
  # another (UiO-66 2026-07-08, PhononWorkChain 7379: max |F| 4.7e-4 Ha/Bohr
  # despite matching k-grid/basis/cutoff). Must keep the seekpath cell,
  # otherwise path_parameters become invalid.
  phonopy_p.structure->phonons:
    validation: aim2dat.cp2k.combined.phonons
    aiida_node: true
    optional: true
    compare: false
  # Port name must be exactly `parameters` (seekpath_structure_analysis's
  # own second argument, aiida_workflows/utils.py:113 -- no aliasing).
  parameters:
    validation: aim2dat.seekpath.structure_analysis
    aiida_node: true
    optional: true
    compare: false
    tasks: [seekpath_analysis]
    value: {reference_distance: 0.015, symprec: 0.005}
  scf_extended_system:
    validation: aim2dat.cp2k.find_scf_p
    aiida_node: true
    compare: true
    tasks: [find_scf_parameters]
  force.scf_extended_system:
    validation: aim2dat.cp2k.combined.phonons
    aiida_node: true
    compare: false
    tasks: [phonons]
  # Default EPS_SCF=1e-8/MAX_SCF=200 -- tighter than find_scf_parameters
  # because SCF noise at 5e-7 caused imaginary modes at zone boundaries (X,
  # L) when force constants are reconstructed from a single displacement.
  force.cp2k.parameters:
    validation: aim2dat.cp2k.combined.phonons
    aiida_node: true
    optional: true
    compare: false
    tasks: [phonons]
    value:
      GLOBAL:
        PRINT_LEVEL: MEDIUM
        PREFERRED_DIAG_LIBRARY: SCALAPACK
        EXTENDED_FFT_LENGTHS: true
      FORCE_EVAL:
        METHOD: Quickstep
        DFT:
          MGRID: {}
          POISSON: {PERIODIC: XYZ, POISSON_SOLVER: PERIODIC}
          BASIS_SET_FILE_NAME: [BASIS_MOLOPT_UZH, BASIS_MOLOPT, BASIS_MOLOPT_UCL]
          POTENTIAL_FILE_NAME: GTH_POTENTIALS
          QS: {EXTRAPOLATION: USE_GUESS, EPS_DEFAULT: 1.0e-14}
          SCF: {EPS_SCF: 1.0e-8, MAX_SCF: 200}
          XC: {}
        SUBSYS:
          CELL: {PERIODIC: XYZ}
  numerical_p.xc_functional:
    validation: aim2dat.cp2k.find_scf_p
    aiida_node: true
    compare: true
    tasks: [find_scf_parameters]
  force.numerical_p.xc_functional:
    validation: aim2dat.cp2k.combined.phonons
    aiida_node: true
    compare: false
    tasks: [phonons]
  numerical_p.basis_sets:
    validation: aim2dat.cp2k.find_scf_p
    aiida_node: true
    compare: true
    tasks: [find_scf_parameters]
  force.numerical_p.basis_sets:
    validation: aim2dat.cp2k.combined.phonons
    aiida_node: true
    compare: false
    tasks: [phonons]
  numerical_p.cutoff_values:
    validation: aim2dat.cp2k.find_scf_p
    aiida_node: true
    compare: true
    tasks: [find_scf_parameters]
  force.numerical_p.cutoff_values:
    validation: aim2dat.cp2k.combined.phonons
    aiida_node: true
    compare: false
    tasks: [phonons]
  cp2k.code:
    validation: aim2dat.cp2k.find_scf_p
    aiida_node: true
    compare: false
    tasks: [find_scf_parameters]
  cp2k.metadata:
    validation: aim2dat.cp2k.find_scf_p
    aiida_node: false
    unstored: true
    optional: true
    compare: false
    tasks: [find_scf_parameters]
  force.cp2k.code:
    validation: aim2dat.cp2k.combined.phonons
    aiida_node: true
    compare: false
    tasks: [phonons]
  force.cp2k.metadata:
    validation: aim2dat.cp2k.combined.phonons
    aiida_node: false
    unstored: true
    optional: true
    compare: false
    tasks: [phonons]
  # Semicore species on production grids need larger displacements for
  # clean force constants (NaCl egg-box case, 2026-07-06).
  phonopy_p.displacement:
    validation: aim2dat.cp2k.combined.phonons
    aiida_node: true
    optional: true
    compare: false
    tasks: [phonons]
    value: 0.01
  phonopy_p.thermal_properties:
    validation: aim2dat.cp2k.combined.phonons
    aiida_node: true
    optional: true
    compare: false
    tasks: [phonons]
    value: false
  phonopy_p.temp_range:
    validation: aim2dat.cp2k.combined.phonons
    aiida_node: true
    optional: true
    compare: false
    tasks: [phonons]
    value: [0.0, 1000.0, 10.0]
  phonopy_p.supercell_matrix:
    validation: aim2dat.cp2k.combined.phonons
    aiida_node: true
    compare: false
    tasks: [phonons]
  clean_workdir:
    validation: aim2dat.cp2k.find_scf_p
    aiida_node: true
    optional: true
    compare: false
    tasks: [find_scf_parameters]
  force.clean_workdir:
    validation: aim2dat.cp2k.combined.phonons
    aiida_node: true
    optional: true
    compare: false
    tasks: [phonons]

results:
  scf_method_level:
    task: find_scf_parameters
    output_port: scf_parameters
    retrieve_value: [method_level]
  scf_parameter_level:
    task: find_scf_parameters
    output_port: scf_parameters
    retrieve_value: [parameter_level]
  scf_smearing_level:
    task: find_scf_parameters
    output_port: scf_parameters
    retrieve_value: [smearing_level]
  phonon_bands:
    task: phonons
    output_port: phonon_bands
  phonon_dos:
    task: phonons
    output_port: phonon_dos
  thermal_properties:
    task: phonons
    output_port: thermal_properties
  ref_forces:
    task: phonons
    output_port: ref_forces
